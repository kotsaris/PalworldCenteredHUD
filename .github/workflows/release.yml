name: release

on:
  push:
    branches:
      - main
    tags:
      - v*

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build archives
        run: |
          zip -r CenteredHUD.zip CenteredHUD
          zip -r TimeSet.zip TimeSet

      - name: Upload CenteredHUD artifact
        uses: actions/upload-artifact@v4
        with:
          name: CenteredHUD
          path: CenteredHUD.zip

      - name: Upload TimeSet artifact
        uses: actions/upload-artifact@v4
        with:
          name: TimeSet
          path: TimeSet.zip

      - name: Create version release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            CenteredHUD.zip
            TimeSet.zip

      - name: Update latest build
        if: github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: Latest build
          prerelease: true
          files: |
            CenteredHUD.zip
            TimeSet.zip
